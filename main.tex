\documentclass[12pt,a4paper,twoside]{tudelft-report}
\usepackage{hyperref}
\usepackage[noabbrev]{cleveref}
\crefname{lstlisting}{listing}{listings}
\Crefname{lstlisting}{Listing}{Listings}
\usepackage{todonotes}
\presetkeys{todonotes}{inline, noline}{}
\usepackage{subcaption}
\usepackage{enumitem}
\usepackage{listings}
\usepackage{hhline}
\usepackage{proof}
\usepackage{usebib} % Use data defined in the bib file.
\bibinput{references}

\graphicspath{{img/}}

% Source code listing style.
\lstset{%
  breaklines=true,
  captionpos=b,
  frame=none,
  keepspaces=true,
  numberstyle=\ttfamily,
  basicstyle=\ttfamily
}

% Table markup:
% 1. Rotate column heads.
% 2. Define check- and crossmarks.
% 3. Top and bottom rules.
\newcommand*\rot{\rotatebox{-50}}
\usepackage{pifont}
\newcommand{\cmark}{\ding{51}}
\newcommand{\xmark}{\ding{55}}
\usepackage{booktabs}

\input{title_preamble}

\lstdefinelanguage{nabl}{
  % list of keywords
  morekeywords={
    namespaces,
    binding, rules,
    imports,
    from,
    implicitly, defines,
    of, type,
    scopes
  },
  sensitive=false, % keywords are not case-sensitive
  morecomment=[l]{//}, % l is for line comment
  morecomment=[s]{/*}{*/}, % s is for start and end delimiter
  morestring=[b]" % defines that strings are enclosed in double quotes
}
\lstdefinelanguage{type-spec}{
  % list of keywords
  morekeywords={
    type, rules,
    where, store, definition, of,
    and, else, on, error
  },
  otherkeywords={@},
  sensitive=false, % keywords are not case-sensitive
  morecomment=[l]{//}, % l is for line comment
  morecomment=[s]{/*}{*/}, % s is for start and end delimiter
  morestring=[b]" % defines that strings are enclosed in double quotes
}
\lstdefinelanguage{stratego}{
  % list of keywords
  morekeywords={
    rules, strategies
  },
  otherkeywords={
    <+, ->, ;, |, \[, \]
  },
  sensitive=false, % keywords are not case-sensitive
  morecomment=[l]{//}, % l is for line comment
  morecomment=[s]{/*}{*/}, % s is for start and end delimiter
  morestring=[b]" % defines that strings are enclosed in double quotes
}
\lstdefinelanguage{dynsem}{
  % list of keywords
  morekeywords={
    signature,
    constructors,
    rules,
    where
  },
  otherkeywords={
    |-, -->, |-->
  },
  sensitive=false, % keywords are not case-sensitive
  morecomment=[l]{//}, % l is for line comment
  morecomment=[s]{/*}{*/}, % s is for start and end delimiter
  morestring=[b]" % defines that strings are enclosed in double quotes
}
\lstdefinelanguage{aterm} {
  morekeywords={Sub,Add,Int},
  otherkeywords={non-unique,[,]}, % {+,*,?}, % ,-,>
  literate={...}{$\ldots$}2,
  comment=[l]{\%\%},
  mathescape=true,
  sensitive=true,
  morestring=[b]" % defines that strings are enclosed in double quotes
}
\lstdefinelanguage{esv}{
  % list of keywords
  morekeywords={
    module,
    shell,
    evaluation, method,
    start, symbol,
    command, prefix
  },
  otherkeywords={
    :
  },
  morestring=[b]" % defines that strings are enclosed in double quotes
}

\begin{document}

\frontmatter

\input{title}

\input{preface}

\input{summary}

\tableofcontents{}

\mainmatter

\input{introduction}

\input{background}

\input{problem-def-ana}

\input{design}

\input{implementation}

\input{evaluation}

\input{discussion}

\input{recommendations}

\input{conclusion}

\appendix

\bibliography{references}{}

\input{infosheet}

\input{project-description}

\input{research-report}

\input{full-uml}

%% Comment out for glossaries
%\glossarystyle{altlistgroup}
%\printglossaries{}

\end{document}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
